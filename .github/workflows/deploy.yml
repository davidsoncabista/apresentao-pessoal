name: Deploy Self-Hosted

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: Checkout do Código
       working-directory: /opt/portfolio-api

    - name: Executar Setup Script
      run: |
           
            ./setup.sh