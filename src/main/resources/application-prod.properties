# Production profile: use MariaDB database
spring.datasource.url=jdbc:mariadb://192.168.0.40:3306/portfolio
spring.datasource.username=portfolio
spring.datasource.password=40F@2Dd%*$^05h
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Use MariaDB dialect and validate schema in production
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
spring.jpa.hibernate.ddl-auto=validate
spring.h2.console.enabled=false

# Production MinIO settings (same as dev for now)
minio.url=http://s3.davidson.dev.br
minio.access-key=java-api
minio.secret-key=ofNl0qGtvr5Fn4
minio.bucket-name=portfolio-image

# Admin credentials for production (change these!)
app.admin.username=${APP_ADMIN_USERNAME:admin}
app.admin.password=${APP_ADMIN_PASSWORD:changeme}

# Redis Cache for production
spring.data.redis.host=192.168.0.51
spring.data.redis.port=6379
spring.data.redis.password=1b6S^nnVa9*GY@

# Telegram integration
telegram.bot.token=${TELEGRAM_BOT_TOKEN}
telegram.chat.id=${TELEGRAM_CHAT_ID}

# Production optimizations
spring.jpa.show-sql=false
logging.level.com.example.demo=INFO